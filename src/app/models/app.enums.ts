export enum GameControlType {
    Dropdown,
    Button
}

export enum GameControlAction {
    NewGame,
    Difficulty,
    Rules
}

export enum Difficulty {
    Easy,
    Medium,
    Hard
}